// 6. Write a program to find the simple Interest.

void main(List<String> args) {
  var principle_amount = 1000, rate_of_interest = 2, time_period = 5;
  var simple_interest = principle_amount * rate_of_interest * time_period / 100;
  print('simple interest is $simple_interest');
}
